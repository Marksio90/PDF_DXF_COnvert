# Used server-side by Next.js rewrites to proxy /api/* to backend.
# Local dev: http://localhost:8000  |  Docker: http://backend:8000
# Railway: https://<backend>.up.railway.app
BACKEND_URL=http://localhost:8000
